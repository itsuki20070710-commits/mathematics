# CLAUDE.md — 開発ガイド（このリポジトリで厳守すること）

個人用「数学 言語化ノート」Webアプリ。ブラウザ完結・個人利用専用。

## 技術スタック（厳守）
- **Vite + React 18 + TypeScript**
- **Tailwind CSS**
- **データ保存: IndexedDB（Dexie）** — ノート本体・画像Blobはすべてここ。サーバ/DB/ログインは持たない
- **設定保存: localStorage** — APIキー・モデル名・表示設定のみ（ノートデータは保存しない）
- **数式表示: KaTeX** — `$...$`（インライン）と `$$...$$`（ディスプレイ）をレンダリング
- **PWA: vite-plugin-pwa** — Phase 4 で有効化（それまで導入しない）
- **デプロイ: GitHub Actions → GitHub Pages** — `vite.config.ts` の `base` はリポジトリ名 `/mathematics/` に合わせる
- **モバイルファースト** — 下部固定タブで画面切替する単一ページSPA

上記スタックから逸脱しない（別のDB・状態管理ライブラリ・CSSフレームワーク等を勝手に足さない）。

## セキュリティ / プライバシー（最重要）
- **APIキーを絶対にコードやリポジトリに含めない。** キーは実行時に localStorage にのみ保存する。
- **ノートデータ（ユーザーの学習内容）を絶対にコミットしない。** データは IndexedDB にのみ存在する。
- `.env` やダミーでない鍵・個人データをコミットしない（`.gitignore` で除外）。

## Git / コミット
- **コミットメッセージは日本語**で、変更内容が分かるように書く。
- **各Phase完了ごとにコミット**する（例:「Phase 1: 骨組み・Dexieデータ層・4画面・KaTeX・Pages自動デプロイ」）。
- 作業は指定の開発ブランチで行い、指示があるまで別ブランチへ push しない・PRを作らない。

## 開発フェーズ
- Phase1: 骨組み＋Dexieデータ層＋登録/ライブラリ/分析/設定＋KaTeX＋Pages自動デプロイ（AIなし）
- Phase2: AI基盤(`src/lib/ai.ts`)＋OCR＋自動分類
- Phase3: AIヒント＋類題生成＋まとめ資料
- Phase4: 復習キュー＋PWA＋UI磨き込み

## メモ
- AI呼び出しは将来 `src/lib/ai.ts` の1モジュールに集約し、差し替え可能にする（Phase 2〜）。
- 詳細な全体仕様は `SPEC.md` を参照。
